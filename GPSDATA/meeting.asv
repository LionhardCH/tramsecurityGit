%%Check if they met

figure
t = 0:.01:2*pi;
x1 = cos(2*t).*(cos(t).^2);
y1 = sin(2*t).*(sin(t).^2);
x2 = sin(2*t).*(sin(t).^2);
y2 = cos(2*t).*(cos(t).^2);

% comet(x1,y1);
hold on
plot(.5,0.2,'ro')
n=length(t);

for i=1:n
plot(x1(i),y1(i),'ro',x2(i),y2(i),'bo');
% axis([-40 60 -600 1000]);
pause(0.1)
hold on
end
% xlim([min(x1),max(x1)]);ylim([min(y1),max(y1)]);
legend('1','2')